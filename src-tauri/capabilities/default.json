{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for local directory selection and app commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
